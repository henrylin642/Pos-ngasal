# POS 點餐系統 PRD (v2.0)

## 1. 專案概述
本專案為因應現代化餐飲管理需求所開發之網頁版 POS (Point of Sale) 系統。旨在取代傳統手寫單流程，提供從前台點餐、廚房出餐到後台管理的完整數位化解決方案。系統採用多店家 (Multi-Tenant) 架構設計，支援單一實例服務多個獨立商家。

### 1.1 核心目標
- **數位化點餐**：以直覺觸控介面取代紙本，降低漏單與人為錯誤。
- **即時資訊同步**：前台訂單即時推送至廚房 KDS (Kitchen Display System)，縮短出餐時間。
- **數據化營運**：自動統計每日營收、熱銷排行，提供業主即時決策依據。
- **多店家支援**：透過店家代碼 (Store Code) 實現資料隔離，支援擴展。

### 1.2 技術架構
- **前端框架**: Next.js 15+ (App Router)
- **UI 系統**: Tailwind CSS + Shadcn UI
- **資料庫**: PostgreSQL (Supabase) + Prisma ORM
- **部署環境**: Vercel (無伺服器架構)

## 2. 系統架構：多店家模式 (Mode A)
系統採用 **Store Code + 帳號** 的登入驗證機制 (`Mode A`)，確保資料嚴格隔離。

- **資料隔離**：所有核心資料表 (`User`, `Category`, `MenuItem`, `Order`, `NoteOption`) 均關聯至 `Store` 模型。
- **登入流程**：
  1. 輸入 **店家代碼 (Store Code)** (例如: `demo`)
  2. 輸入 **使用者帳號** (例如: `admin`)
  3. 輸入 **密碼**
  4. 系統驗證店家代碼是否存在 -> 驗證該店家下之使用者帳號密碼。

## 3. 使用者角色與權限

| 角色 | 代號 | 職責與權限 |
| :--- | :--- | :--- |
| **業主 (Admin)** | `ADMIN` | 擁有完整權限。可管理菜單、查看營收報表、管理員工帳號、備註設定。亦可執行前台點餐與廚房視圖。 |
| **廚房 (Kitchen)** | `KITCHEN` | 專注於 KDS 廚房顯示系統。接收訂單、變更餐點狀態 (製作中/已完成)。 |
| **前台 (Front)** | `FRONT` | 負責點餐與結帳。建立新訂單、送單至廚房、結帳收款。 |

## 4. 核心功能模組

### 4.1 前台點餐系統 (POS)
- **菜單瀏覽**：支援分類顯示 (主餐、飲品等)，清楚展示餐點名稱與價格。
- **購物車管理**：
  - 調整數量、刪除品項。
  - **客製化備註**：支援預設備註選項 (如：去冰、小辣) 或手動輸入。
- **訂單建立**：
  - **內用 (Dine In)**：需選擇桌號 (視覺化桌位狀態：空閒/用餐中)。
  - **外帶 (Take Out)**：無需桌號。
- **結帳流程**：計算總金額，確認付款後結案。

### 4.2 廚房顯示系統 (KDS)
- **即時看板**：新訂單自動顯示，無需手動刷新。
- **狀態流轉**：
  - **待處理 (Pending)**：新進單，提醒廚房接單。
  - **製作中 (Cooking)**：廚房開始製作。
  - **已完成 (Completed)**：餐點製作完畢，通知出餐。
- **分類檢視**：支援「進行中」與「歷史紀錄」分頁，可查詢當日已完成之訂單。
- **視覺輔助**：不同狀態以顏色區分 (如：紅色緊急、綠色完成)。

### 4.3 後台管理系統 (Dashboard)
- **營運總覽**：
  - **今日數據**：訂單數、總營收、客單價。
  - **趨勢比較**：與昨日或上月同期相比之成長率。
- **視覺化報表**：
  - **營收圖表**：本月每日營收趨勢圖。
  - **熱銷排行**：前 5 名熱賣商品與銷售量。
- **銷售日曆**：
  - **月曆視圖**：每日營收概況。
  - **詳細清單**：點擊日期可查看該日所有訂單明細。
  - **天氣整合**：整合每日天氣資訊 (Open-Meteo)，輔助分析天氣對營收之影響。
- **菜單管理**：
  - **分類管理**：新增/修改/刪除/排序菜單分類。
  - **餐點管理**：新增/修改/刪除餐點，設定價格、圖片(網址)、是否上架。
  - **備註管理**：設定全域通用之備註選項。
- **資料備份**：支援匯出該店家所有資料 (JSON 格式) 以供備份。

## 5. 資料庫模型 (Schema)
核心模型概覽：
- `Store`: 店家實體 (Unique Code)。
- `User`: 員工帳號，複合唯一鍵 `[storeId, username]`。
- `Category` / `MenuItem`: 菜單結構。
- `Order` / `OrderItem`: 交易紀錄，包含狀態流轉。
- `NoteOption`: 客製化選項庫。

## 6. 未來規劃 (Roadmap)
- [ ] **客戶端點餐 (BYOD)**：掃描 QR Code 讓顧客用手機自助點餐。
- [ ] **進階庫存管理**：扣料機制與低庫存提醒。
- [ ] **支付串接**：整合第三方金流 (Line Pay, 信用卡)。
- [ ] **多語言支援**：介面多語系切換。

---
*文件更新日期: 2026-01-12*
